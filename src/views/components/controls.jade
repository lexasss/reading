//- requires:

mixin controls(id)
    div(id=id).controls
        span.device Connecting
        .buttonBlock.etud
            .button.disabled.options Options
            .button.disabled.calibrate Calibrate
            .button.disabled.toggle Start
        .separator
        .buttonBlock.text
        .separator
        .buttonBlock.spacing
        .separator
        .button.loadSession Load session
        .button.loadCondition Load condition
        .button.loadFile Load file
